# p1nano-host — пульт управления маком из iCON P1-Nano

Превращает свободный DAW-слот контроллера **iCON P1-Nano** в **пульт управления macOS**:
моторный фейдер = двусторонний регулятор системной громкости, кнопки = транспорт/Mute/запуск
приложений, энкодеры = яркость и громкость. Плюс HUD-плашка и индикатор громкости на LCD.

Хост по протоколу **Mackie Control Universal (MCU)** «прикидывается DAW», будит устройство
на выбранном порту и работает с ним напрямую через CoreMIDI. Ableton при этом не нужен —
слот просто должен быть в режиме, дающем фейдеру pitch-bend (Ableton Live / Mackie Control).

## Возможности

- **Фейдер → громкость.** Ведёшь фейдер → громкость мака плавно, полное разрешение
  (CoreAudio), верх = 100 %.
- **Громкость → фейдер.** Внешнее изменение громкости → моторный фейдер сам едет.
- **HUD-плашка** громкости на экране (приватный `OSDManager`).
- **LCD-индикатор** — устройство показывает текущую громкость числом + полоской.
- **Кнопки:** Play/Pause, пред./след. трек, Mute, мьют микрофона, запуск приложений, макросы.
- **Энкодеры:** яркость экрана (с плашкой), громкость (относительные V-Pot).
- **Сенсорный экран** (4″): кнопки можно повесить на запуск приложений/команд.
- **Медиа без прав:** Play/треки через MediaRemote, яркость через DisplayServices — без Accessibility.
- **Привязки в конфиге** `~/.config/p1nano/mapping.conf` — меняются без пересборки.
- **Без клацанья мотора:** позиция шлётся только при изменении, в покое тихо.
- **Автозапуск** (LaunchAgent) — поднимается при логине, сам цепляется к iCON.

Полная таблица привязок и как их менять — в [`MAPPING.md`](MAPPING.md).

## Запуск

Переключи P1-Nano на свободный DAW-слот (у меня **DAW 3 = Ableton Live** → даёт фейдеру
pitch-bend на «Порт 3»). Затем:

```bash
bash start.sh
```

Хост можно запускать даже до включения iCON — он ждёт устройство и подключается сам.
Остановить: `Ctrl+C`.

Запуск вручную (с подробным логом):

```bash
DBG=1 ./host "Порт 3"      # на железе
DBG=1 ./host "P1Sim"       # против локального симулятора
```

## Как это устроено

### Протокол / «пробуждение» устройства
- Хост шлёт устройству непрерывный MCU-поток: метеры (`F0 00 00 66 14 20 …`),
  запрос версии прошивки (`14 13`), текст на LCD (`14 12`). Это «будит» слот — на экране
  появляется «Mac Volume», и фейдер начинает слать свою позицию.
- Физический фейдер шлёт **Pitch Wheel на канале 1**; касание фейдера = нота **104 (G♯6)**.
- Кнопки транспорта в режиме Ableton шлют ноты **93 (Stop) / 94 (Play)** (в MIDI-мониторе
  отображаются как `A5` / `A♯5`).

### Громкость
- Ставится мгновенно через **CoreAudio** (`kAudioDevicePropertyVolumeScalar`), без
  запуска `osascript` — поэтому плавно и без лагов.

### HUD-плашка
- `OSDManager.showImage:onDisplayID:priority:msecUntilFade:filledChiclets:totalChiclets:locked:`
  (image = 3 — динамик, 16 «чиклетов»). Вызов через IMP метода (типы `q,I,I,I,I,I,B`).
  Работает на macOS 26 (Tahoe).

### Play/Pause
- `NSEvent.otherEvent(.systemDefined)` с `NX_KEYTYPE_PLAY = 16` → `CGEvent` на
  `.cghidEventTap`.

### Архитектура потоков (ключевое — из-за этого раньше падало)
Первая версия дёргала CoreAudio/AppKit/OSDManager **прямо из MIDI-callback’а**. AppKit не
потокобезопасен — при быстром потоке pitch это роняло процесс («работает, потом умирает»).
Текущая версия разделена:

- **MIDI-поток** (CoreMIDI callback) — только парсит байты настоящим потоковым парсером
  (с поддержкой **running-status**: быстрый фейдер слипает несколько pitch-сообщений в один
  пакет) и кладёт состояние под `NSLock`. Никаких тяжёлых вызовов отсюда.
- **Главный поток**, таймер **30 Гц** — забирает последнее состояние и делает всю работу:
  ставит громкость, рисует HUD, шлёт Play/Pause, удерживает мотор. Берётся только последнее
  значение фейдера → плавно, без флуда.
- Отдельные таймеры: обратная связь громкость→фейдер (10 Гц) и keepalive/online (4 Гц).

## Автозапуск

```bash
bash install-autostart.sh        # поставить в автозапуск (LaunchAgent), сразу стартует
bash uninstall-autostart.sh      # убрать
```

Хост поднимается при логине, сам держится живым (`KeepAlive`) и подключается к iCON через
**MIDINotify**, когда устройство включат — независимо от порядка. Лог: `/tmp/p1nano-host.log`.

> **Права для медиа-клавиш.** Play/треки/яркость постят системные события и требуют
> разрешения **System Settings → Privacy & Security → Accessibility** (добавить бинарник
> `host`). Громкость/фейдер/LCD/Mute работают и без этого.

## Файлы

| Файл | Назначение |
|---|---|
| `host.swift` | Хост (главный). Сборка: `swiftc host.swift -o host`. |
| `devicesim.swift` | Локальный симулятор P1-Nano (виртуальный порт `P1Sim`) — тесты без железа. |
| `start.sh` | Запуск на железе (Порт 3). |
| `install-autostart.sh` / `uninstall-autostart.sh` | Автозапуск (LaunchAgent). |
| `test.sh` / `fbtest.sh` / `suitetest.sh` | Бесшумные тесты (DRYRUN). |
| `MAPPING.md` | Привязки кнопок/энкодеров + настройка сенсорного экрана. |
| `mapping.conf.example` | Шаблон конфига привязок (копия в `~/.config/p1nano/mapping.conf`). |
| `NEXT-IDEAS.md` | Идеи дальнейшего развития. |
| `device-config/` | Бэкап рабочего конфига устройства (`P1Nano.conf`). |

Бинарники `host` / `devicesim` не коммитятся — собираются из `.swift`.

## Бесшумные тесты (без железа, не трогают звук)

В режиме `DRYRUN=1` громкость/Mute хранятся в файлах-заглушках (`/tmp/p1nano_fakevol`),
реальный мак НЕ трогается; медиа-клавиши/HUD/запуск приложений становятся no-op. Поэтому
тесты можно гонять даже когда играет музыка.

```bash
bash test.sh       # фейдер -> громкость  (диапазон ~7..92)
bash fbtest.sh     # громкость -> фейдер  (мотор едет на 20/80/35)
bash suitetest.sh  # кнопки + энкодеры    (Play/треки/Mute/запуск/яркость/громкость)
```

## Настройка

- **Порт.** Аргумент хоста — подстрока имени MIDI-порта (`"Порт 3"`). Совпадает с DAW-слотом.
- **Привязки кнопок/энкодеров.** Таблицы `NOTE_ACTIONS` / `ENC_TARGETS` в начале `host.swift`,
  подробности — `MAPPING.md`. Хост логирует каждую нажатую ноту → легко найти и поменять.
- На DAW 1 (Ableton с MackieControlFollow) хост одновременно не запускать — это другой слот,
  чтобы не делить физический фейдер.

## Зависимости

macOS, Swift (Xcode CLT). Только системные фреймворки: CoreMIDI, CoreAudio, AppKit,
приватный OSD.framework. Внешних пакетов нет.
</content>
